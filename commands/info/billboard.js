const { MessageEmbed } = require("discord.js");

module.exports = {
  name: "billboard",
  category: "info",
  description: "show Billboard Chart",
  run: async (client, message, args) => {
      message.reply("this function is not completed")
  },
};
